<!--shopcart.wxml-->
<view wx:if='{{shopcart.length == 0}}' class='m-cart-empty'>
  <view class='u-circle'></view>
  <text>去添加点什么吧</text>
</view>
<view wx:else class='page container'>
  <view class='page__bd'>
    <view class='u-shopcart-item weui-cells weui-cells_after-title'>
        <checkbox-group bindchange="checkboxChange">
                <label class="weui-cell weui-check__label" wx:for="{{checkboxItems}}" wx:key="value">
                    <checkbox class="weui-check" value="{{item.value}}" checked="{{item.checked}}"/>

                    <view class="weui-cell__hd weui-check__hd_in-checkbox">
                        <icon class="weui-icon-checkbox_circle" type="circle" size="23" wx:if="{{!item.checked}}"></icon>
                        <icon class="weui-icon-checkbox_success" type="success" size="23" wx:if="{{item.checked}}"></icon>
                    </view>
                    <view class="weui-cell__bd">
                      <image class='u-shopcart-thumbnails' src='{{item.thumbnails}}'></image>
        <text>{{item.productAccount}}</text>
                    </view>
                </label>
            </checkbox-group>
      </view>
  </view>
</view>
